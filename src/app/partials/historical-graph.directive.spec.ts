import { HistoricalGraphDirective } from './historical-graph.directive';

describe('HistoricalGraphDirective', () => {
  it('should create an instance', () => {
    const directive = new HistoricalGraphDirective();
    expect(directive).toBeTruthy();
  });
});
